apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-k8s-app.fullname" . }}
  labels:
    {{- include "python-k8s-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-k8s-app.selectorLabels" . | nindent 4 }}
